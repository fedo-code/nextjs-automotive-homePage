# nextjs-automotive-homePage

Dynamic luxury automotive website with MongoDB-powered admin content management.

## Tech Stack

- Next.js (App Router)
- React
- MongoDB
- NextAuth

## Getting Started

1. Install dependencies:

```bash
npm install
```

2. Configure environment variables:

- Copy `.env.example` values into `.env.local`
- Add all required auth and database secrets

3. Start development server:

```bash
npm run dev
```

4. Open `http://localhost:3000`

## Scripts

- `npm run dev` - start development server
- `npm run build` - production build
- `npm run start` - run production build
- `npm run lint` - run lint checks

## Notes

- API route ownership and namespace rules are documented in `app/api/README.md`.
- Detailed setup steps are available in `SETUP.md`.







